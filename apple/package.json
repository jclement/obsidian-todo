{
  "name": "obsidian-todo-ios",
  "version": "0.0.0",
  "private": true,
  "description": "Native iOS (Capacitor) wrapper for Obsidian Todo — a thin WKWebView shell that loads the live web app, for persistent microphone permission and passkeys.",
  "scripts": {
    "add:ios": "cap add ios",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^7",
    "@capacitor/ios": "^7"
  },
  "devDependencies": {
    "@capacitor/cli": "^7"
  }
}
